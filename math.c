#include <stdio.h>
#include <math.h>

int main(){
    double A = sqrt(9);
    double B = pow(2, 3);
    double C = round(3.14);

    printf("Sqrt of 9 is: %f \n", A);
    printf("2^3 is: %f \n", B);
    printf("3.14 round to nearest integer is: %f \n", C);

    return 0;
}